#include <iostream>
#include <ctime>

using namespace std;
/*
int main() {
	int arr[50] = {};
	srand(time(NULL));
	for (int i = 0; i < 50; ++i) {
		int random_number = rand() % 100;
		while (random_number % 2 == 0)
			random_number = rand() % 100;
		arr[i] = random_number;
	}

	for (int num : arr)
		cout << num << " ";
} */