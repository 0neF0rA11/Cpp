#include <iostream>

using namespace std;
/*
int main() 
{
	char str[128] = "";
	cin.getline(str, 128);
	int char_code, index = 0;
	while (str[index] != '\0') 
	{
		char_code = int(str[index]);
		if (char_code == 122) 
		{
			str[index] = char(97);
		}
		else if (char_code == 90) 
		{
			str[index] == char(65);
		}
		else if (char_code >= 65 && char_code <= 90 || char_code >= 97 && char_code <= 122)
		{
			str[index] = char(char_code + 1);
		}
		index++;
	}

	for (char chr : str)
		cout << chr;
}
*/